    // Vertical alignement for Safari 4 and 5
    var checkOldSafari = String(navigator.userAgent.match(/Version\/[\d\.]+.*Safari/));
    var res = String(checkOldSafari.match(/[\d\.]+/));
    var newres = parseInt(res.substring(0, 1));
    if(newres < 6){
      /**
      * Enter code here
      */
    }
